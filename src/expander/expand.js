import { Cons } from "../core/cons.js";

// just returns the s-expression tree produced by the reader for now
/**
 *
 * @param {Cons} prog
 * @returns {Cons}
 */
export function expand(prog) {
  return prog;
}
